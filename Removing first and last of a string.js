const removeChar = str => str.slice(1, str.length - 1 )


function removeChar(str){
    newStr = str.slice(1, str.length - 1)
    return newStr
}
//In JavaScript you can use -1 to fetch back end of a string.