const areaOrPerimeter = function(l , w) {
    if(l === w){
      return l*w
    }else{
      return (l*2) + (w*2)
    }
  };


// const areaOrPerimeter = function(l , w) {
//  return l == w ? l*w : 2*(l + w)
//};
// looking to keep track of clever solutions